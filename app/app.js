document.addEventListener("DOMContentLoaded",
    function loadTreeData() {
        var xhr = new XMLHttpRequest();
        xhr.open('GET', '../tree.json', false);

        xhr.onload = function() {
                return new Promise(function(resolve) {

                });
        };
        //xhr.send();
});




        
